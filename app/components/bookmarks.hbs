<div class="bookmarks">
    
    {{#if this.isGreen}}
        <button
            ...attributes
            type="button" 
            id = {{@id}}
            class="image" {{on "click" this.usaServicio}}>    
            <small>📕</small>
        </button>
    {{else}}
        <button
            ...attributes 
            type="button" 
            id = {{@id}}
            class="image" {{on "click" this.usaServicio}}>
            <small>📗</small>
        </button>
    {{/if}}
</div>