<div class="bookmarks">
    
    {{#if this.isRed}}
        <button
            ...attributes
            type="button" 
            id = {{@id}}
            class="image" {{on "click" this.useService}}>    
            <small>📕</small>
        </button>
    {{else}}
        <button
            ...attributes 
            type="button" 
            id = {{@id}}
            class="image" {{on "click" this.useService}}>
            <small>📗</small>
        </button>
    {{/if}}
</div>