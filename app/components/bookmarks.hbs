<div class="bookmarks">
    
    {{#if this.isLarge}}
        <button
            ...attributes
            type="button" 
            id={{@id}} 
            class="image" {{on "click" this.toggleBookmark}}>
            <small>📕</small>
        </button>
    {{else}}
        <button
            ...attributes 
            type="button" 
            id={{@id}} 
            class="image" {{on "click" this.toggleBookmark}}>
            <small>📗</small>
        </button>
    {{/if}}
</div>